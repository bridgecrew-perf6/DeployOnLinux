* tailwind was not working in login.blade.php in jetstream. solution : `npm run hot`
* if photo doesn't show, run : `php artisan storage:link`
  * change .env APP_URL=http://localhost:8000 //http://jetstream_project.test
